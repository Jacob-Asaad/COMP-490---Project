import {API_KEY} from "@env"

export default{
    API_KEY,
}

// To use these variables in other locations, 
// make sure to
// import config from "./config"
//
// Have a file in the root of the project called .env and
// initialize the variables you want, example below
// API_KEY=abc123
//
//access the variable by {config.API_KEY}